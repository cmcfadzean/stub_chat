<%= form_with(model: member) do |form| %>
  <% if member.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(member.errors.count, "error") %> prohibited this member from being saved:</h2>

      <ul>
        <% member.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.collection_select(:user_id, User.where.not(id: current_user.id), :id, :email) %>
  </div>

  <div class="field">
    <%= form.label :group_id %>
    <%= form.text_field :group_id, :value => params[:group] %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
